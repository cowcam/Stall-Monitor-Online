# Serve specific HTML files for exact matches (prevents fallback)
/setup              /setup.html         200

# Handle assets
/assets/* /assets/:splat      200

# SPA Fallback: Serve dashboard.html for any path not matched above,
# keeping the original URL. This handles /dashboard/FarmName AND
# implicitly handles the root / by serving index.html by default.
/* /dashboard.html     200