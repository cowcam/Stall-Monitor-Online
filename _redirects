# Specific paths/assets first (prevents catch-all override)
/                   /index.html             200
/setup              /setup.html             200
/assets/* /assets/:splat          200

# Catch-all for everything else, fallback to dashboard.html
/* /dashboard.html         200