# Handle the dashboard rewrite first. This will serve dashboard.html
# for any path starting with /dashboard/, like /dashboard/AnyFarmName.
/dashboard/* /dashboard.html 200

# Handle specific page routes.
/setup /setup.html 200

# Handle the root path.
/ /index.html 200

# Serve assets from the assets folder.
/assets/* /assets/:splat 200
